# TODO - Backend/ fix all file and folder

## Plan implemented steps
- [x] Fix case-sensitive model import by adding `backend/src/models/userModel.js` re-export

- [x] Remove insecure/debug console logs and ensure field mappings in `authController.js`

- [x] Ensure correct model import usage in `gameController.js`
- [x] Ensure correct model import usage in `adminController.js`


- [x] Check frontend `frontend/src/services/api.js` for any backend route/header mismatches and fix if needed
- [x] Run backend (`npm run dev`) and hit `/api/health` to verify boot


